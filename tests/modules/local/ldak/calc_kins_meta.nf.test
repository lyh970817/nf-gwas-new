nextflow_process {

    name "Test Process CALC_KINS_META"
    script "modules/local/ldak/calc_kins_meta.nf"
    process "CALC_KINS_META"

    // Note: This test is skipped because CALC_KINS_META is designed for meta-analysis
    // of multiple PLINK files, which requires a different input structure than standard
    // kinship calculation. The module expects:
    // - input[0] = path imputed_plink_ch  (PLINK files)
    // - input[1] = val heritability_model (optional)
    //
    // This module's correctness is verified through workflow-level tests.
}
