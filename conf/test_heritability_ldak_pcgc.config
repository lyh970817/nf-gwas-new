/*
========================================================================================
    Test profile for LDAK PCGC heritability estimation (binary traits)
========================================================================================
    Liability-scale heritability for case-control studies using PCGC regression.
    Requires prevalence parameter.
*/

params {
    // Enable heritability estimation
    run_association_analysis      = false
    run_heritability_estimation   = true
    run_genetic_correlation       = false

    // Use LDAK PCGC method
    heritability_method           = 'ldak_pcgc'
    heritability_model            = 'human'

    // Override to use binary phenotype
    phenotypes_filename           = "$baseDir/tests/input/phenotype_bin.txt"
    phenotypes_binary_trait       = true

    // PCGC-specific parameters
    ldak_pcgc_prevalence          = 0.2   // 20% disease prevalence
}

process {
    withName: '.*' {
        cpus = 2
        memory = 2.GB
    }
}
